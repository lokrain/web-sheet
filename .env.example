# Copy this file to .env.local and fill in values.
#
# Next.js loads .env* files automatically for the app runtime.
# The XML benchmark script (`npm run bench`) loads `.env.local` and `.env` via dotenv.
# Jest loads `.env.test.local` and `.env.test` via dotenv.
#
# Optional: Path to an external XML fixture used by `npm run bench`.
# If set and the file exists, it will be included in the benchmark run.
#
# XML_BENCH_FILE=/absolute/path/to/fixture.xml
